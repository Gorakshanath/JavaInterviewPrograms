version: '3'

services:

  keycloak:
    image: quay.io/keycloak/keycloak:26.2

    command: start-dev

    ports:
      - "8080:8080"

    environment:
      KEYCLOAK_ADMIN:
      KEYCLOAK_ADMIN_PASSWORD: